/**
 * Expense routes for handling expense tracking functionality
 */

import express from 'express';

const router = express.Router();

// Register expense routes function
export function registerExpenseRoutes(app) {
  // Placeholder implementation - add expense routes here
  app.use('/api/expenses', router);
}