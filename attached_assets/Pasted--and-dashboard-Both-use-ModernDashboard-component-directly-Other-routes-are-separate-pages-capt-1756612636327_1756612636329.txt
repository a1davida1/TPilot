/ and /dashboard → Both use ModernDashboard component directly
Other routes are separate pages (caption-generator, reddit, settings, etc.)
All those other dashboard files are UNUSED JUNK!

The Good News:
Your routing is actually clean! You're using ModernDashboard for the main dashboard.
The Problems I See:

Admin not recognized properly:

typescriptconst isAdmin = user && (user.id === 999 || user.username === 'admin' || user.isAdmin);
This is checking, but the user object might not have these fields set correctly.

ModernDashboard gets props but might not use them:

typescript<ModernDashboard user={user} userTier={userTier} isAdmin={isAdmin} />

🧹 Clean Up Command for Replit Agent:
We have identified the routing structure. ModernDashboard is the only dashboard being used.

CLEANUP TASKS:

1. DELETE all unused dashboard files:
```bash
# Move unused dashboards to backup
mkdir -p client/src/components/DASHBOARD_BACKUP
mv client/src/components/unified-dashboard.tsx client/src/components/DASHBOARD_BACKUP/
mv client/src/components/sidebar-dashboard.tsx client/src/components/DASHBOARD_BACKUP/
mv client/src/components/queue-dashboard.tsx client/src/components/DASHBOARD_BACKUP/
mv client/src/components/enhanced-dashboard.tsx client/src/components/DASHBOARD_BACKUP/
mv client/src/components/clean-dashboard.tsx client/src/components/DASHBOARD_BACKUP/
mv client/src/components/analytics-dashboard.tsx client/src/components/DASHBOARD_BACKUP/
# Keep BillingDashboard if it's used elsewhere

FIX ModernDashboard to use the props:
In client/src/components/modern-dashboard.tsx, make sure it accepts and uses props:

typescriptinterface ModernDashboardProps {
  user?: any;
  userTier?: string;
  isAdmin?: boolean;
}

export function ModernDashboard({ user, userTier = 'free', isAdmin = false }: ModernDashboardProps) {
  // Use isAdmin to unlock features
  const isPremium = isAdmin || userTier === 'premium' || userTier === 'pro';
  
  // Debug log
  console.log('Dashboard props:', { user, userTier, isAdmin, isPremium });
  
  // Rest of component...
}

FIX feature gating in ModernDashboard:
Find any disabled buttons/features and update:

typescript// Instead of:
disabled={!user || user.tier === 'free'}

// Use:
disabled={!isPremium}

FIX the Reddit OAuth redirect:
All OAuth callbacks should redirect to '/dashboard' or '/' (both go to same place)
ADD debug info to dashboard:
Temporarily add this to ModernDashboard to see what's happening:

typescript{/* Debug Info - Remove after fixing */}
<div className="fixed bottom-4 right-4 bg-black text-white p-4 rounded text-xs">
  <div>User ID: {user?.id}</div>
  <div>Username: {user?.username}</div>
  <div>Tier: {userTier}</div>
  <div>Is Admin: {isAdmin ? 'YES' : 'NO'}</div>
</div>

---

## 🔍 **Quick Debug in Browser:**

Run this in console to see what user data you have:

```javascript
// Check localStorage
console.log('Auth Token:', localStorage.getItem('authToken'));
console.log('User:', JSON.parse(localStorage.getItem('user') || '{}'));

// Force admin mode temporarily
const adminUser = {
  id: 999,
  username: 'admin',
  email: 'admin@thottopilot.com',
  tier: 'premium',
  isAdmin: true
};
localStorage.setItem('user', JSON.stringify(adminUser));
window.location.reload();